<div class="container tag-show">
    <h3>list for <%= @tag.name %></h3>
    <ul>
        <% @tag.taggings.each do |tag| %>
            <% if tag.article.image.exists? %>
                <li><%= link_to image_tag tag.article.image.url %></li>
                <li><%=  tag.article.body %></li>
            <% else %>
                <li><%=  tag.article.body %></li>
            <% end %>
        <% end %>
    </ul>
</div>